class Solution:
    def canBeEqual(self, target: List[int], arr: List[int]) -> bool:
        ans = True
        for i in arr :
            if i in target :
                target.remove(i)
                continue
            else :
                ans = False
                break
        return ans
        
        
