def solution(n):
    answer = 0             # ** 는 실수형 반환
    if n ** 0.5 % 1 == 0 : #n의 제곱근이 1로 나누었을 때 나머지가 없다면 n은 x의 제곱
        return ((n ** 0.5) + 1) ** 2 # x+1 의 제곱 리턴
    return -1 # 양의 정수 x의 제곱이 아니면 -1 리턴
